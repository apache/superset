# Claude Code RC for issue32143

This is a claudette-managed Apache Superset development environment.

## Project: issue32143
- Worktree Path: /Users/evan_1/.claudette/worktrees/issue32143
- Frontend Port: 9008
- Frontend URL: http://localhost:9008

## Quick Commands

Start services:
```bash
claudette docker up
```

Access frontend:
```bash
open http://localhost:9008
```

Run tests:
```bash
# Backend
pytest tests/unit_tests/

# Frontend
cd superset-frontend && npm test
```

## Environment Details
- Python venv: `.venv/` (auto-activated in claudette shell)
- Node modules: `superset-frontend/node_modules/`
- Docker prefix: `issue32143_`

## Development Tips
- Always use `claudette shell` to work in this project
- Run `pre-commit run --all-files` before committing
- Use `claudette docker` instead of docker-compose directly
- The frontend dev server runs on port 9008 to avoid conflicts
