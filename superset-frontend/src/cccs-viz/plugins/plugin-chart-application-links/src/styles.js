const InlineBlock = {
  display: 'inline-block',
};

const InlineImg = {
  display: 'inline-block',
  transform: 'translateY(-10%)',
  '-ms-transform': 'translateY(-10%)',
};

const InlineText = {
  display: 'inline-block',
  'text-align': 'center',
};

const styles = {
  InlineBlock,
  InlineImg,
  InlineText,
};

export default styles;
