export type RequestListType = {
  id: number;
  user: string;
  // firstName: string;
  // lastName: string;
  email: string;
  requestedRoles: string;
  team: string;
  requestDate: Date;
  isClosed: boolean;
};
