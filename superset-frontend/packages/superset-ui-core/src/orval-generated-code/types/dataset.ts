/**
 * Generated by orval v7.11.2 🍺
 * Do not edit manually.
 * Superset
 * Superset
 * OpenAPI spec version: v1
 */
import type { DatasetColumnFormats } from './datasetColumnFormats';
import type { DatasetColumnsItem } from './datasetColumnsItem';
import type { DatasetDatabase } from './datasetDatabase';
import type { DatasetExtra } from './datasetExtra';
import type { DatasetGranularitySqlaItemItem } from './datasetGranularitySqlaItemItem';
import type { DatasetMetricsItem } from './datasetMetricsItem';
import type { DatasetParams } from './datasetParams';
import type { DatasetTemplateParams } from './datasetTemplateParams';
import type { DatasetVerboseMap } from './datasetVerboseMap';

export interface Dataset {
  /** Duration (in seconds) of the caching timeout for this dataset. */
  cache_timeout?: number;
  /** Column formats. */
  column_formats?: DatasetColumnFormats;
  /** Columns metadata. */
  columns?: DatasetColumnsItem[];
  /** Database associated with the dataset. */
  database?: DatasetDatabase;
  /** Dataset name. */
  datasource_name?: string;
  /** Default endpoint for the dataset. */
  default_endpoint?: string;
  /** Dataset description. */
  description?: string;
  /** The URL for editing the dataset. */
  edit_url?: string;
  /** JSON string containing extra configuration elements. */
  extra?: DatasetExtra;
  /** Predicate used when fetching values from the dataset. */
  fetch_values_predicate?: string;
  /** SELECT filter applied to the dataset. */
  filter_select?: boolean;
  /** If the SELECT filter is enabled. */
  filter_select_enabled?: boolean;
  /** Name of temporal column used for time filtering for SQL datasources. This field is deprecated, use `granularity` instead. */
  granularity_sqla?: DatasetGranularitySqlaItemItem[][];
  /** Health check message. */
  health_check_message?: string;
  /** Dataset ID. */
  id?: number;
  /** If the dataset is a SQL Lab view. */
  is_sqllab_view?: boolean;
  /** The main temporal column. */
  main_dttm_col?: string;
  /** Dataset metrics. */
  metrics?: DatasetMetricsItem[];
  /** Dataset name. */
  name?: string;
  /** Dataset offset. */
  offset?: number;
  /** List of order by columns. */
  order_by_choices?: string[][];
  /** List of owners identifiers */
  owners?: number[];
  /** Extra params for the dataset. */
  params?: DatasetParams;
  /** Permission expression. */
  perm?: string;
  /** Dataset schema. */
  schema?: string;
  /** Select all clause. */
  select_star?: string;
  /** A SQL statement that defines the dataset. */
  sql?: string;
  /** The name of the table associated with the dataset. */
  table_name?: string;
  /** Table template params. */
  template_params?: DatasetTemplateParams;
  /** List of temporal granularities supported by the dataset. */
  time_grain_sqla?: string[][];
  /** Dataset type. */
  type?: string;
  /** Dataset unique identifier. */
  uid?: string;
  /** Mapping from raw name to verbose name. */
  verbose_map?: DatasetVerboseMap;
}
