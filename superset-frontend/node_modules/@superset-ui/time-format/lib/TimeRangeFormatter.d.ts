import { ExtensibleFunction } from '@superset-ui/core';
import { TimeRangeFormatFunction } from './types';
interface TimeRangeFormatter {
    (value: (Date | number | null | undefined)[]): string;
}
declare class TimeRangeFormatter extends ExtensibleFunction {
    id: string;
    label: string;
    description: string;
    formatFunc: TimeRangeFormatFunction;
    useLocalTime: boolean;
    constructor(config: {
        id: string;
        label?: string;
        description?: string;
        formatFunc: TimeRangeFormatFunction;
        useLocalTime?: boolean;
    });
    format(values: (Date | number | null | undefined)[]): string;
}
export default TimeRangeFormatter;
//# sourceMappingURL=TimeRangeFormatter.d.ts.map