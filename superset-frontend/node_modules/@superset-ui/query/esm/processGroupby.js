"use strict";

exports.__esModule = true;
exports.default = processGroupby;

function processGroupby(groupby) {
  const groupbyList = [];
  groupby.forEach(value => {
    if (typeof value === 'string') {
      groupbyList.push(value);
    }
  });
  return groupbyList;
}