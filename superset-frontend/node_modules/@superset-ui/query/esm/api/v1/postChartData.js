"use strict";

exports.__esModule = true;
exports.default = postChartData;

var _connection = require("@superset-ui/connection");

function _extends() { _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; }; return _extends.apply(this, arguments); }

function postChartData({
  client = _connection.SupersetClient,
  requestConfig,
  queryContext
}) {
  return client.post(_extends({}, requestConfig, {
    endpoint: '/api/v1/chart/data',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify(queryContext)
  })).then(({
    json
  }) => json);
}