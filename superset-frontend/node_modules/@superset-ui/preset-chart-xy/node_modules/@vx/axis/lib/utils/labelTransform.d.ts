import { TextProps } from '@vx/text/lib/Text';
import { AxisOrientation, ScaleOutput } from '../types';
export interface TransformArgs {
    labelOffset: number;
    labelProps: Partial<TextProps>;
    orientation: AxisOrientation;
    range: ScaleOutput[];
    tickLabelFontSize: number;
    tickLength: number;
}
export default function labelTransform({ labelOffset, labelProps, orientation, range, tickLabelFontSize, tickLength, }: TransformArgs): {
    x: number;
    y: number;
    transform: string | undefined;
};
//# sourceMappingURL=labelTransform.d.ts.map