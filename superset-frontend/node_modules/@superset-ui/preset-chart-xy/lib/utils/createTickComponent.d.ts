import { CSSProperties } from 'react';
import { AxisLayout } from './computeAxisLayout';
export default function createTickComponent({ axisWidth, labelAngle, labelFlush, labelOverlap, orient, tickLabels, tickLabelDimensions, tickTextAnchor, }: AxisLayout): (({ x, y, dy, formattedValue, ...textStyle }: {
    x: number;
    y: number;
    dy?: import("@superset-ui/connection").ClientTimeout;
    formattedValue: string;
    textStyle: CSSProperties;
}) => JSX.Element) | null;
//# sourceMappingURL=createTickComponent.d.ts.map