import * as ts from 'typescript';
export interface Options {
    sourcemap?: boolean;
    autoLabel?: boolean;
    labelFormat?: string;
    autoInject?: boolean;
    customStyledModuleName?: string;
}
export interface ImportInfos {
    name: string;
    type: 'namedImport' | 'namespaceImport' | 'defaultImport';
    moduleName: string;
}
export declare const createEmotionPlugin: (options?: Options | undefined) => ts.TransformerFactory<ts.SourceFile>;
