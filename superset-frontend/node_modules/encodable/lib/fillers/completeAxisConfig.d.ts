import { RequiredSome } from '../types/Base';
import { ChannelType } from '../types/Channel';
import { AxisConfig, LabelOverlapStrategy } from '../types/Axis';
import { HalfCompleteChannelDef } from '../types/CompleteChannelDef';
export declare type CompleteAxisConfig = false | (RequiredSome<Omit<AxisConfig, 'labelOverlap'>, 'labelAngle' | 'labelFlush' | 'labelPadding' | 'orient' | 'tickCount' | 'ticks' | 'title' | 'titlePadding'> & {
    labelOverlap: LabelOverlapStrategy;
});
export default function completeAxisConfig(channelType: ChannelType, channelDef: HalfCompleteChannelDef): CompleteAxisConfig;
//# sourceMappingURL=completeAxisConfig.d.ts.map