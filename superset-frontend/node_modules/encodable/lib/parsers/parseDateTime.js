"use strict";

exports.__esModule = true;
exports.default = parseDateTime;

var _datetime = require("vega-lite/build/src/datetime");

function parseDateTime(dateTime) {
  if (typeof dateTime === 'number' || typeof dateTime === 'string') {
    return new Date(dateTime);
  }

  return new Date((0, _datetime.dateTimeToTimestamp)(dateTime));
}