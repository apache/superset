import { ChannelInput } from '../../types/Channel';
import { ScaleType } from '../../types/VegaLite';
/**
 * Convert each element in the array into
 * - Date (for time scales)
 * - number (for other continuous scales)
 * @param domain
 * @param scaleType
 */
export default function parseContinuousDomain<T extends ChannelInput>(domain: T[], scaleType: ScaleType): Exclude<T, string | number | boolean>[] | Exclude<T, string | boolean>[];
//# sourceMappingURL=parseContinuousDomain.d.ts.map