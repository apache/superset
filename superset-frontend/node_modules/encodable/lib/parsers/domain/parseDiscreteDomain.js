"use strict";

exports.__esModule = true;
exports.default = parseDiscreteDomain;

/**
 * Discrete domains are converted into string[]
 * when using D3 scales
 * @param domain
 */
function parseDiscreteDomain(domain) {
  return domain.map(d => "" + d);
}