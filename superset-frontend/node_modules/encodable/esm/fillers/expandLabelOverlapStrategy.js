"use strict";

exports.__esModule = true;
exports.default = expandLabelOverlapStrategy;
exports.STRATEGY_FLAT = void 0;

var _Channel = require("../typeGuards/Channel");

const STRATEGY_FLAT = {
  strategy: 'flat'
};
exports.STRATEGY_FLAT = STRATEGY_FLAT;
const STRATEGY_ROTATE = {
  labelAngle: 40,
  strategy: 'rotate'
};

function expandLabelOverlapStrategy(channelType, labelOverlap = 'auto') {
  let output;

  switch (labelOverlap) {
    case 'flat':
      output = STRATEGY_FLAT;
      break;

    case 'rotate':
      output = STRATEGY_ROTATE;
      break;

    case 'auto':
      output = (0, _Channel.isX)(channelType) ? STRATEGY_ROTATE : STRATEGY_FLAT;
      break;

    default:
      output = labelOverlap;
      break;
  }

  return { ...output
  };
}