"use strict";

exports.__esModule = true;
exports.default = completeLegendConfig;

var _Channel = require("../typeGuards/Channel");

function completeLegendConfig(channelType, channelDef) {
  if ('legend' in channelDef && channelDef.legend !== undefined) {
    return channelDef.legend;
  }

  return (0, _Channel.isXOrY)(channelType) || channelType === 'Text' ? false : {};
}