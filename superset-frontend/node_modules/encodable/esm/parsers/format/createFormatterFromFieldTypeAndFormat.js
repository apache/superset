"use strict";

exports.__esModule = true;
exports.default = createFormatterFromFieldTypeAndFormat;

var _numberFormat = require("@superset-ui/number-format");

var _timeFormat = require("@superset-ui/time-format");

var _fallbackFormatter = _interopRequireDefault(require("./fallbackFormatter"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function createFormatterFromFieldTypeAndFormat(type, format) {
  if (type === 'quantitative') {
    const formatter = (0, _numberFormat.getNumberFormatter)(format);
    return value => formatter(value);
  }

  if (type === 'temporal') {
    const formatter = (0, _timeFormat.getTimeFormatter)(format);
    return value => formatter(value);
  }

  return _fallbackFormatter.default;
}