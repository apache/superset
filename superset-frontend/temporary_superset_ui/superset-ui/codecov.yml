coverage:
  status:
    patch:
      default:
        target: 0%
        threshold: 0%
      core-packages:
        target: 0%
        paths:
          - packages
      plugins:
        target: 0%
        paths:
          - plugins
    project:
      default:
        target: 0%
        # send notification if coverage change exceed this threshold
        threshold: 0%
      core-packages:
        target: 100%
        paths:
          - packages
      plugins:
        target: 0%
        paths:
          - plugins
