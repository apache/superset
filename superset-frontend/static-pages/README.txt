These pages are compiled into self contained static html, to be easily served by nginx with no dependencies.

After making changes to a template file, be sure to run `python3 generate_pages.py` to update the generated output. 

Do not make direct edits to the generated files.

