// DODO was here

export enum ValueToShowEnum {
  OLDEST = 'oldest',
  AVERAGE = 'average',
  LATEST = 'latest',
}

export enum AlignmentName {
  LEFT = 'left',
  CENTER = 'center',
  RIGHT = 'right',
}

export enum AlignmentValue {
  LEFT = 'flex-start',
  CENTER = 'center',
  RIGHT = 'flex-end',
}
