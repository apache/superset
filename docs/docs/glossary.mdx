---
title: Glossary
hide_title: true
sidebar_position: 10
---

import { Glossary, GlossaryStructure } from '@superset-ui/core';
import { Table, ConfigProvider } from 'antd';
import { theme } from "antd";
import { useColorMode } from '@docusaurus/theme-common';

export const GlossaryContent = () => {
  const { colorMode } = useColorMode();
  const isDark = colorMode === 'dark';
  
  return (
    <ConfigProvider
      theme={{
        algorithm: isDark
        ? theme.darkAlgorithm
        : theme.defaultAlgorithm,
      }}
    >
      <Table 
        dataSource={Glossary} 
        columns={GlossaryStructure}
        pagination={false}
        pagination={false}
        showHeader={false}
        bordered={true}
      />
    </ConfigProvider>
  );
};

## Glossary

<GlossaryContent />