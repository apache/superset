---
title: Content Localization
hide_title: true
sidebar_position: 13
version: 1
---

# Content Localization

Superset supports localization of user-created content: dashboard titles, chart names, descriptions,
metric labels, axis titles, annotation names, and more. When a viewer's UI language changes,
translated content is displayed automatically.

:::note
Requires the `ENABLE_CONTENT_LOCALIZATION` [feature flag](/docs/configuration/feature-flags)
and a database migration (`1af0da0adfec`).
:::

## Enabling Content Localization

### 1. Enable the Feature Flag

```python
# superset_config.py
FEATURE_FLAGS = {
    "ENABLE_CONTENT_LOCALIZATION": True,
}
```

### 2. Configure Available Languages

Content localization uses the same `LANGUAGES` config as UI translations:

```python
# superset_config.py
LANGUAGES = {
    "en": {"flag": "us", "name": "English"},
    "de": {"flag": "de", "name": "German"},
    "fr": {"flag": "fr", "name": "French"},
    "ru": {"flag": "ru", "name": "Russian"},
}
```

### 3. Run the Database Migration

The migration adds a `translations` JSON column to the `dashboards` and `slices` tables:

```bash
superset db upgrade
```

## How It Works

### Translation Storage

Translations are stored as JSON alongside the original content:

```json
{
  "dashboard_title": {
    "de": "Verkaufs-Dashboard",
    "fr": "Tableau de bord des ventes"
  },
  "description": {
    "de": "Monatlicher Verkaufsbericht"
  }
}
```

Each entity (dashboard, chart) has a `translations` field. Native filter translations
are stored inside `json_metadata.native_filter_configuration[].translations`.

### Locale Detection

When a user views a dashboard, the API resolves the locale using this priority chain:

1. **Explicit locale parameter** (API override, if provided)
2. **Session locale** (`session["locale"]`, set via language switcher or `/lang/<locale>`)
3. **`Accept-Language` header** (browser preference)
4. **Config default** (`BABEL_DEFAULT_LOCALE`)

### Fallback Chain

If a translation is missing for the resolved locale:

1. Exact locale match (e.g., `pt-BR`)
2. Base language (e.g., `pt`)
3. Original field value

## Adding Translations

A globe icon (🌐) with a badge counter appears next to translatable fields in edit mode.
The badge shows how many translations exist. Click the globe to open a language dropdown,
select a language, and type the translation. Select **DEFAULT** to return to the base text.

### Properties Modals

#### Dashboard Title and Description

1. Enter edit mode (**Edit dashboard** button) &rarr; open the **&hellip;** menu &rarr; **Edit properties**
2. Click the globe icon (🌐) inside the Title or Description field
3. Select a language, type the translation
4. Click **Save**

Translatable fields: `dashboard_title`, `description`

#### Chart Name and Description

1. Open **Edit Chart Properties** from the chart menu
2. Click the globe icon (🌐) inside the Name or Description field
3. Select a language and enter the translation
4. Click **Save**

Translatable fields: `slice_name`, `description`

#### Native Filter Name

1. Open the filter bar &rarr; click the gear icon to configure filters
2. Select a filter and click the globe icon (🌐) inside the filter name field
3. Select a language and enter the translated filter name
4. Click **Save**

Translatable field: `name`

### Inline Title Editors

In edit mode, a globe icon appears next to inline title editors:

- **Dashboard title** &mdash; the main title at the top of the dashboard
- **Chart title in Explore** &mdash; the chart name above the visualization
- **Chart name override on dashboards** &mdash; the per-dashboard override of a chart's name
- **Tab names** &mdash; dashboard tab labels

Click the globe, select a language, edit the title, and save. Translations are stored
alongside the original content and applied automatically for viewers.

The display priority chain for chart names on dashboards:

1. Override translation for viewer's locale
2. Override name (untranslated)
3. Chart translation for viewer's locale
4. Chart original name

### Chart Controls in Explore

#### Adhoc Metric Labels

Click a metric pill (e.g., `COUNT(*)`) to open the metric editor. The globe icon
in the popover title lets you translate the metric's display label. Translated labels
appear in chart tooltips, legends, axis labels, and table headers across 20 chart types.

#### Adhoc Column Labels

Click a Custom SQL column pill to open the column editor. When a custom label is set,
the globe icon appears in the popover title for translation. Translated labels appear
in Table and Pivot Table column headers.

#### Axis Titles

The **X Axis Title** and **Y Axis Title** fields (in the Customize tab) support translations.
Click the globe icon inside the input field to switch languages.

Supported charts: Timeseries (Line, Bar, Area, Scatter, Step), MixedTimeseries (including
secondary Y axis), Bubble, BoxPlot, Gantt, Histogram, Waterfall.

#### BigNumber Subtitle and Compare Suffix

The **Subtitle** field on BigNumber charts (Total, Trendline, Period Over Period) and the
**Compare Suffix** field on BigNumber With Trendline support translations via the globe icon.

#### Waterfall Labels

The **Increase Label**, **Decrease Label**, and **Total Label** fields on Waterfall charts
support translations. These labels appear in the chart legend.

#### Annotation Names

Annotation layer names on Timeseries and MixedTimeseries charts support translations.
Translated names appear in the chart legend and tooltips.

### Chart Visualizations

Translated metric labels are applied automatically in these chart types:

| Chart | Where translated labels appear |
|-------|-------------------------------|
| Table | Metric and column headers |
| Pivot Table | Metric values, row/column headers |
| Pie | Tooltips |
| Funnel | Tooltips |
| Gauge | Center label, tooltips |
| Radar | Axis indicator labels |
| Treemap | Root node, tooltips |
| Sunburst | Tooltips (primary and secondary metrics) |
| Bubble | All 3 axes (X, Y, size) |
| Heatmap | Series name, tooltips |
| Graph | Edge weight |
| Tree | Node metric |
| Sankey | Flow value |
| BoxPlot | Series, tooltips |
| Gantt | Tooltips |
| BigNumber Total | Main value |
| BigNumber With Trendline | Main value |
| BigNumber Period Over Period | Main value |
| Timeseries | Tooltips, legend |
| MixedTimeseries | Tooltips, legend |

## API

### Response Modes

The API supports two modes controlled by the `include_translations` query parameter.

**Default mode** &mdash; returns localized field values:

```
GET /api/v1/dashboard/123
```

When the user's session locale is `de` (set via the language switcher), the response
returns localized values:

```json
{
  "dashboard_title": "Verkaufs-Dashboard",
  "available_locales": ["de", "fr"]
}
```

**Editor mode** &mdash; returns original values with full translations dict:

```
GET /api/v1/dashboard/123?include_translations=true
```

```json
{
  "dashboard_title": "Sales Dashboard",
  "translations": {
    "dashboard_title": {"de": "Verkaufs-Dashboard", "fr": "Tableau de bord"}
  },
  "available_locales": ["de", "fr"]
}
```

### Saving Translations

Include the `translations` field in a standard PUT request:

```
PUT /api/v1/dashboard/123
Content-Type: application/json

{
  "translations": {
    "dashboard_title": {
      "de": "Verkaufs-Dashboard",
      "fr": "Tableau de bord des ventes"
    }
  }
}
```

The same pattern applies to charts via `PUT /api/v1/chart/{id}`.

### Available Locales Endpoint

```
GET /api/v1/localization/available_locales
```

```json
{
  "result": {
    "locales": [
      {"code": "de", "name": "German", "flag": "de"},
      {"code": "en", "name": "English", "flag": "us"}
    ],
    "default_locale": "en"
  }
}
```

## SQL Templating

When `ENABLE_TEMPLATE_PROCESSING` is enabled, the `{{ current_user_locale() }}` Jinja macro
returns the viewer's resolved locale. This enables locale-aware SQL queries:

```sql
SELECT product_name
FROM products
WHERE language = '{{ current_user_locale() }}'
```

The locale value is automatically included in the cache key. To disable cache key inclusion:

```sql
{{ current_user_locale(add_to_cache_keys=False) }}
```

See [SQL Templating](/docs/configuration/sql-templating) for more Jinja macros.

## Embedded Dashboards

The [Embedded SDK](https://www.npmjs.com/package/@superset-ui/embedded-sdk) supports
locale configuration at initialization and dynamic switching at runtime:

```js
import { embedDashboard } from "@superset-ui/embedded-sdk";

const dashboard = await embedDashboard({
  id: "abc123",
  supersetDomain: "https://superset.example.com",
  mountPoint: document.getElementById("my-superset-container"),
  fetchGuestToken: () => fetchGuestTokenFromBackend(),
  initialLocale: "de", // optional: set locale at load time
});

// Switch locale dynamically when host app language changes
languageSelector.onChange = (locale) => {
  dashboard.setLocale(locale);
};
```

## Export and Import

Translations are included in dashboard and chart YAML exports:

```yaml
dashboard_title: Sales Dashboard
description: Monthly sales report
translations:
  dashboard_title:
    de: Verkaufs-Dashboard
    fr: Tableau de bord des ventes
  description:
    de: Monatlicher Verkaufsbericht
```

Importing a YAML without the `translations` field sets translations to `null` (backward compatible).

## Configuration Limits

Protect against oversized payloads with these settings in `superset_config.py`:

| Setting | Default | Description |
|---------|---------|-------------|
| `CONTENT_LOCALIZATION_MAX_LOCALES` | 50 | Max unique locales per entity |
| `CONTENT_LOCALIZATION_MAX_TEXT_LENGTH` | 10000 | Max characters per translation value |
| `CONTENT_LOCALIZATION_MAX_FIELD_LENGTH` | 50 | Max characters for field names |
| `CONTENT_LOCALIZATION_MAX_JSON_SIZE` | 1048576 | Max translations JSON size in bytes (1 MB) |

## Security

- **Permissions**: Translation editing follows entity permissions. Users who can edit a dashboard
  can edit its translations.
- **XSS prevention**: All translation values are sanitized on save (HTML tags stripped).
- **Validation**: Locale codes are validated against BCP 47 / POSIX formats. Field names and
  values are validated against configured size limits.
- **Feature flag guard**: `PUT` requests with `translations` are rejected when the feature flag
  is disabled.
