{
  "compilerOptions": {
    // syntax rules
    "strict": true,

    "moduleResolution": "bundler",

    // environment
    "target": "es6",
    "lib": ["DOM", "ESNext"],
    "module": "esnext",

    // output
    "outDir": "./dist",
    "emitDeclarationOnly": true,
    "declaration": true
  },

  "include": [
    "src/**/*"
  ],

  "exclude": [
    "dist",
    "lib",
    "node_modules"
  ]
}
