#
# STAGING OVERRIDE
# Defines settings unique to the staging/testing environment.
#
services:
  superset:
    ports:
      - "8089:8088" # Map internal port 8088 to host port 8089

  db:
    ports:
      - "5433:5432" # Staging database on port 5433 to avoid production conflict