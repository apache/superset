{{- if .Values.supersetWorker.autoScale.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ template "superset.fullname" . }}-hpa-worker
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ template "superset.fullname" . }}-worker
    chart: {{ template "superset.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  minReplicas: {{ .Values.supersetWorker.autoScale.minReplicas }}
  maxReplicas: {{ .Values.supersetWorker.autoScale.maxReplicas }}
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ template "superset.fullname" . }}-worker
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        averageUtilization: {{ .Values.supersetWorker.autoScale.averageUtilizationCPU }}
        type: Utilization
  - type: Resource
    resource:
      name: memory
      target:
        averageUtilization: {{ .Values.supersetWorker.autoScale.averageUtilizationMEMORY }}
        type: Utilization
{{- end }}