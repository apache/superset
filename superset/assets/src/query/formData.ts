// Type signature and utility functions for formData
// It will be gradually filled out as we build out the query object
export interface FormData {
  datasource: string;
  groupby?: string[];
  series?: string;
}
