import { NormalizedMessage } from '../NormalizedMessage';
/**
 * Creates new default formatter.
 *
 * @returns {defaultFormatter}
 */
export declare function createDefaultFormatter(): (message: NormalizedMessage, useColors: boolean) => string;
