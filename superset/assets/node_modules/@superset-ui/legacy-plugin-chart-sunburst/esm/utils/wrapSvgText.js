/* eslint-disable babel/no-invalid-this, no-plusplus */ /*
  Utility function that takes a d3 svg:text selection and a max width, and splits the
  text's text across multiple tspan lines such that any given line does not exceed max width

  If text does not span multiple lines AND adjustedY is passed,
  will set the text to the passed val
*/import d3 from"d3";export default function wrapSvgText(a,b,c){// ems
a.each(function(){for(var a,d=d3.select(this),e=d.text().split(/\s+/),f=[],g=0,h=d.attr("x"),j=d.attr("y"),k=parseFloat(d.attr("dy")),l=d.text(null).append("tspan").attr("x",h).attr("y",j).attr("dy",k+"em"),m=!1,n=0;n<e.length;n++)a=e[n],f.push(a),l.text(f.join(" ")),l.node().getComputedTextLength()>b&&(f.pop(),l.text(f.join(" ")),f=[a],l=d.append("tspan").attr("x",h).attr("y",j).attr("dy",++g*1+k+"em").text(a),m=!0);m||"undefined"==typeof c||l.attr("y",c)})}