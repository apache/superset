import { AdhocMetric, MetricKey } from './Metric';
declare type Metrics = Partial<Record<MetricKey, AdhocMetric | string>>;
declare type BaseFormData = {
    datasource: string;
} & Metrics;
declare type SqlaFormData = {
    granularity_sqla: string;
} & BaseFormData;
declare type DruidFormData = {
    granularity: string;
} & BaseFormData;
export declare type FormData = SqlaFormData | DruidFormData;
export {};
