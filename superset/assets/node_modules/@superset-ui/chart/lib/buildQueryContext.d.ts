import { QueryObject } from './buildQueryObject';
import FormData from './FormData';
export default function buildQueryContext(formData: FormData, buildQuery?: (baseQueryObject: QueryObject) => QueryObject[]): {
    datasource: {
        id: number;
        type: import("./DatasourceKey").DatasourceType;
    };
    queries: QueryObject[];
};
