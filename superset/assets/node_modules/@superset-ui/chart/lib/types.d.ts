import ChartProps from './models/ChartProps';
import { Metric } from './query/Metric';
export declare enum DatasourceType {
    Table = "table",
    Druid = "druid"
}
export interface QueryObject {
    granularity: string;
    groupby?: string[];
    metrics?: Metric[];
}
export interface QueryContext {
    datasource: {
        id: number;
        type: DatasourceType;
    };
    queries: Array<QueryObject>;
}
export declare type BuildQueryFunction<T extends FormData> = (formData: T) => QueryContext;
export declare type TransformPropsFunction = (chartProps: ChartProps) => {
    [key: string]: any;
};
