import { FormDataMetric, MetricKey } from './Metric';
declare type Metrics = Partial<Record<MetricKey, FormDataMetric | FormDataMetric[]>>;
export declare type AnnotationLayerMetadata = {
    name: string;
    sourceType?: string;
};
declare type BaseFormData = {
    datasource: string;
    viz_type: string;
    annotation_layers?: Array<AnnotationLayerMetadata>;
} & Metrics;
declare type SqlaFormData = {
    granularity_sqla: string;
} & BaseFormData;
declare type DruidFormData = {
    granularity: string;
} & BaseFormData;
export declare type FormData = SqlaFormData | DruidFormData;
export {};
