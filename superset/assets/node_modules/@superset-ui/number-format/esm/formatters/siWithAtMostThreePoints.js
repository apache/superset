import"core-js/modules/es6.regexp.to-string";/* eslint-disable sort-keys */import{format as d3Format}from"d3-format";import NumberFormatter from"../NumberFormatter";var siFormatter=d3Format(".3s"),formatter=new NumberFormatter({name:"si_with_at_most_three_points",label:"SI with <=3 decimal points",formatFn:function c(a){var b=siFormatter(a);// Removing trailing `.00` if any
return"A">b.slice(-1)&&(b=parseFloat(b).toString()),b}});export default formatter;