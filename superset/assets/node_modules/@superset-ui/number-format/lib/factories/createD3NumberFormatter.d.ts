import NumberFormatter from '../NumberFormatter';
export default function createD3NumberFormatter(config: {
    description?: string;
    formatString: string;
    label?: string;
}): NumberFormatter;
