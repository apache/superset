import { ExtensibleFunction } from '@superset-ui/core';
import { NumberFormatFunction } from './types';
export declare const PREVIEW_VALUE = 12345.432;
export interface NumberFormatterConfig {
    id: string;
    label?: string;
    description?: string;
    formatFunc: NumberFormatFunction;
    isInvalid?: boolean;
}
export default class NumberFormatter extends ExtensibleFunction {
    id: string;
    label: string;
    description: string;
    formatFunc: NumberFormatFunction;
    isInvalid: boolean;
    constructor(config: NumberFormatterConfig);
    format(value: number | null | undefined): string;
    preview(value?: number): string;
}
