import 'whatwg-fetch';
import { CallApi } from '../types';
export default function callApi({ body, cache, credentials, headers, method, mode, postPayload, redirect, signal, stringify, url, }: CallApi): Promise<Response>;
