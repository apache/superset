apiVersion: v1
kind: Service
metadata:
  name: superset-svc
  namespace: "default"
  labels:
    name: superset
    component: primary
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8088
      protocol: TCP
      targetPort: 8088
  selector:
    name: postgresql
    component: primary
