apiVersion: v1
kind: Service
metadata:
  name: vault-postgresql
  namespace: "default"
  labels:
    name: postgresql
    component: primary
spec:
  type: ClusterIP
  ports:
    - name: tcp-postgresql
      port: 5432
      targetPort: tcp-postgresql
      nodePort: null
  selector:
    name: postgresql
    component: primary
