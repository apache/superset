apiVersion: v1
kind: Service
metadata:
  name: vault-redis-master
  namespace: "default"
  labels:
    name: redis
    component: master
spec:
  type: ClusterIP
  ports:
    - name: tcp-redis
      port: 6379
      targetPort: redis
      nodePort: null
  selector:
    name: redis
    component: master
