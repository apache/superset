apiVersion: v1
kind: Service
metadata:
  name: vault-redis-headless
  namespace: "default"
  labels:
    name: redis
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: tcp-redis
      port: 6379
      targetPort: redis
  selector:
    name: redis
