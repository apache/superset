apiVersion: v1
kind: Service
metadata:
  name: vault-superset-celerybeat
  labels:
    app: superset
spec:
  type: ClusterIP
  ports:
    - port: 443
      protocol: TCP
  selector:
    app: superset
